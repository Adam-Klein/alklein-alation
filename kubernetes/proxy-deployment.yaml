apiVersion: apps/v1
kind: Deployment
metadata:
  name: proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: proxy
  strategy: {}
  template:
    metadata:
      labels:
        io.kompose.service: proxy
    spec:
      containers:
      - image: alklein/alklein_alation_proxy
        imagePullPolicy: "IfNotPresent"
        name: proxy
        ports:
        - containerPort: 80
        resources: {}
      restartPolicy: Always
      serviceAccountName: ""
